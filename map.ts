let marks: number[] = [1, 2, 3, 4, 5];
// marks = [45];
const doubles: number[] = marks.map((mark, index) => mark * 2);
console.log(marks);

let nums: number[] = [1, 2, 3, 4, 5, 6];
const dbls: number[] = nums.map(num => num * 2);
// nums = [5];
console.log(dbls);

export { };